<Window x:Class="assignment.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:assignment.Views"
        mc:Ignorable="d"
        Title="MainView" Height="1060" Width="1200">
    <Grid>
        <!-- Row Definitions -->
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Column Definitions -->
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Wafer Map Viewer -->
        <local:OpenFile Grid.Row="1" Grid.Column="0"/>
        <local:WaferMap Grid.Row="0" Grid.Column="0" Margin="48,0,48.8,9.6"/>
        <local:DefectList Grid.Row="1" Grid.Column="1"/>
    </Grid>
</Window>
